# skills-agent-lib

Portable agentic skills library with init scaffolding.

## Installation

```bash
pip install .
```

## Usage

Initialize a new project with the `.agents` structure:

```bash
skills-agent init
```

List available skills:

```bash
skills-agent list
```

Add a specific skill:

```bash
skills-agent add <skill-name>
```
